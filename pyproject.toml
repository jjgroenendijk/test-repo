[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "requests>=2.34.1",
    "spotiflac>=0.5.0",
    "uvicorn>=0.46.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "playwright>=1.59.0",
    "pytest>=9.0.2",
    "ruff>=0.15.10",
]
