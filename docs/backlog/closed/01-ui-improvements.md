# Improve job card styling

The job cards have inline styles that are hard to maintain. We need to extract them to the CSS stylesheet (`website/src/styles.css`).
And improve the overall styling and layout of the page.
