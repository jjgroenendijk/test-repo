# Add System Info Endpoint

A system info endpoint would provide useful metadata about the running instance, such as backend version, platform details, and storage limits.

## Requirements
- Add a simple `/api/system/info` endpoint.
- Include a mock backend version (e.g., `1.0.0`).
- Ensure it works with CORS and returns 200 OK.
- Write a test for the endpoint.
