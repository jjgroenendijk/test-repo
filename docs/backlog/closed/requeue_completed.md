# Re-queue functionality for completed jobs

Add a "Re-queue" button to completed jobs, similar to the "Retry" button on failed/cancelled jobs. Clicking it should queue the same URL again. This improves user experience when needing to re-download a previously completed item.
